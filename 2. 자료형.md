# 🧑‍🏫 JavaScript 자료형(Data Types)

---

## 📌 1. 자바스크립트 자료형이란?

> **자료형(Data Type)** 이란 변수에 저장할 수 있는 **값의 종류**를 의미합니다.
> 자바스크립트는 **동적 타입 언어**로, 변수에 저장되는 값에 따라 자료형이 결정됩니다.

```javascript
let x = 10;         // Number
x = "hello";        // String
x = true;           // Boolean
```

✅ 변수는 선언 시 타입이 정해지지 않고, **값에 따라 언제든 바뀔 수 있음**

---

## 🧩 2. 자바스크립트 자료형 분류

### ✅ 기본형(Primitive Type)

| 자료형         | 설명                     |
| ----------- | ---------------------- |
| `Number`    | 숫자 (정수/실수 포함)          |
| `String`    | 문자열                    |
| `Boolean`   | 참/거짓 (`true`, `false`) |
| `Undefined` | 정의되지 않은 값              |
| `Null`      | 명시적인 빈 값               |
| `BigInt`    | 매우 큰 정수                |
| `Symbol`    | 고유하고 변경 불가능한 식별자       |

### ✅ 참조형(Reference Type)

| 자료형                | 설명                |
| ------------------ | ----------------- |
| `Object`           | 키-값 쌍을 저장하는 구조    |
| `Array`            | 순서가 있는 값의 집합 (객체) |
| `Function`         | 함수도 객체            |
| `Date`, `RegExp` 등 | 내장 객체             |

---

## 🧮 3. typeof 연산자

```javascript
console.log(typeof 42);           // "number"
console.log(typeof "hello");     // "string"
console.log(typeof true);        // "boolean"
console.log(typeof undefined);   // "undefined"
console.log(typeof null);        // ❗️"object" (오래된 버그)
console.log(typeof {});          // "object"
console.log(typeof []);          // "object"
console.log(typeof function(){});// "function"
```

✅ `typeof`는 값의 자료형을 확인할 수 있는 연산자입니다.
❗ `typeof null === "object"`는 자바스크립트의 오래된 설계 오류입니다.

---

## 🔢 4. 숫자(Number)

```javascript
let a = 123;
let b = 3.14;
let c = -Infinity;
let d = NaN;
```

* 자바스크립트는 **정수/실수 구분 없이 모두 `number` 타입**
* `NaN`: Not a Number → 연산 불가 상태
* `Infinity`, `-Infinity`: 무한대 값

---

## 🔤 5. 문자열(String)

```javascript
let msg = "Hello";
let name = 'Alice';
let greet = `Hi, ${name}!`; // 템플릿 문자열
```

* 문자열은 `"`, `'`, `` ` `` (백틱)으로 감쌀 수 있음
* 백틱은 **템플릿 리터럴**로 **문자열 보간**이 가능함

---

## 🔘 6. 불리언(Boolean)

```javascript
let isLoggedIn = true;
let isAdmin = false;
```

* 논리 값: `true`, `false`
* 조건문, 반복문, 제어문 등에 필수

---

## 🫥 7. undefined vs null

| 값           | 의미              | typeof 결과     |
| ----------- | --------------- | ------------- |
| `undefined` | 변수는 선언됐지만 값이 없음 | `"undefined"` |
| `null`      | **의도적으로 비어있음**  | `"object"` ❗️ |

```javascript
let x;
console.log(x);        // undefined

let y = null;
console.log(y);        // null
```

---

## 🔢 8. BigInt

```javascript
let big = 1234567890123456789012345678901234567890n;
console.log(typeof big); // "bigint"
```

* `n`을 붙여서 생성
* 아주 큰 정수를 정확하게 표현할 수 있음
* `number`와 `bigint`는 연산 불가 → 별도로 처리해야 함

---

## 🧿 9. Symbol

```javascript
let id = Symbol("id");
let another = Symbol("id");

console.log(id === another); // false
```

* **유일한 식별자**를 만들 때 사용
* 객체의 숨겨진 키로도 활용됨
* 보통 외부에서 접근 못하게 하는 용도로 사용

---

## 📦 10. 객체(Object)

```javascript
const user = {
  name: "Tom",
  age: 30,
  greet() {
    console.log("Hello");
  }
};
```

```javascript
const user = {
  name: "Tom",
  age: 30,
  hobbies: ["축구", "게임", "영화감상"],
  // 1. 가입 날짜 (Date 객체 사용)
  createdAt: new Date(), 
  // 2. 생일 (특정 날짜 지정)
  birthday: new Date("1994-05-20"),
  
  greet: function() {
    console.log("환영합니다");
  }
};

// 확인 방법
console.log(user.name);
console.log(user.age);
console.log(user.hobbies);
console.log(user.hobbies[0]);
console.log(user.createdAt); // 현재 날짜와 시간 출력
console.log(user.birthday.getFullYear()); // 1994 (연도만 추출 가능)
console.log(user.greet());
// 수정 방법
user.name = "swseo";
console.log(user.name);
user.age = 51;
console.log(user.age);
user.hobbies[0] = "농구";
console.log(user.hobbies[0]);

```

* 키(key) - 값(value) 쌍으로 구성된 **복합 데이터 구조**
* 배열, 함수, 날짜 등도 객체의 특수한 형태

---

## 🧱 11. 배열(Array)

```javascript
const list = [1, 2, 3, 4];
console.log(typeof list); // "object"
console.log(Array.isArray(list)); // true
```

* 배열은 객체의 한 종류 → `typeof`는 `"object"`
* 배열인지 확인하려면 `Array.isArray()` 사용

---

## 🔁 12. 형 변환(Type Conversion)

### 명시적 변환

```javascript
String(123);       // "123"
Number("3.14");    // 3.14
Boolean(0);        // false
```

### 암묵적 변환 (자동 변환)

```javascript
"5" + 1       // "51" (문자열)
console.log("var1=" + var1 + "," + "type=" + typeof var1);

"5" - 1       // 4    (숫자)
true + 1      // 2
false == 0    // true (느슨한 비교)
false === 0   // false (엄격한 비교)
```

---

## 🧪 실습 문제

### Q1. 다음 중 `typeof` 연산 결과가 `"object"`가 아닌 것은?

A. `[]`
B. `{}`
C. `function() {}`
D. `Symbol("id")`

<details>
<summary>✅ 정답 보기</summary>
정답: D  
`typeof []`, `{}` → `"object"`  
`typeof function() {}` → `"function"`  
`typeof Symbol("id")` → `"symbol"`
</details>

---

### Q2. 다음 코드의 출력 결과는?

```javascript
console.log(typeof null);   // ?
console.log(typeof NaN);    // ?
```

<details>
<summary>✅ 정답 보기</summary>
- `typeof null` → `"object"` (버그)  
- `typeof NaN` → `"number"`
</details>

---

## 🎯 정리: 자료형 일람

| 유형        | 예시                         | typeof 결과                  |
| --------- | -------------------------- | -------------------------- |
| Number    | `123`, `3.14`, `NaN`       | `"number"`                 |
| String    | `"hello"`, `'world'`       | `"string"`                 |
| Boolean   | `true`, `false`            | `"boolean"`                |
| Undefined | `let x;`                   | `"undefined"`              |
| Null      | `let x = null;`            | `"object"` ❗️              |
| BigInt    | `123n`                     | `"bigint"`                 |
| Symbol    | `Symbol("id")`             | `"symbol"`                 |
| Object    | `{}`, `[]`, `function(){}` | `"object"` 또는 `"function"` |

