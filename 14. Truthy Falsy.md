# 📚 JavaScript Truthy와 Falsy
자바스크립트의 `Truthy`와 `Falsy`는 **조건문에서 값이 암묵적으로 true 또는 false로 평가되는 성질**을 의미합니다.
이는 자바스크립트의 강제 형 변환(Coercion)에 기반한 개념으로, 조건식에서 어떤 값을 넣었을 때 자동으로 Boolean(true/false)로 변환되는 원리를 이해해야 합니다.
---

## ✅ Truthy / Falsy란?

| 용어         | 의미                         |
| ---------- | -------------------------- |
| **Truthy** | 조건식에서 자동으로 `true`로 평가되는 값  |
| **Falsy**  | 조건식에서 자동으로 `false`로 평가되는 값 |

```javascript
if (value) {
  // value가 Truthy일 경우 이 블록이 실행됨
}
```

---

## ❌ Falsy 값 목록 (단 7개)

자바스크립트에서 `false`로 평가되는 값은 다음 7개뿐입니다:

| 값           | 설명                    |
| ----------- | --------------------- |
| `false`     | 불리언 false 자체          |
| `0`         | 숫자 0                  |
| `-0`        | 음수 0 (별도 취급)          |
| `0n`        | BigInt 0              |
| `""`        | 빈 문자열 (''도 포함)        |
| `null`      | null                  |
| `undefined` | 정의되지 않은 값             |
| `NaN`       | 숫자가 아님 (Not a Number) |

```javascript
if (0)          → false
if (null)       → false
if ("")         → false
if (undefined)  → false
```

---

## ✅ Truthy 값 예시 (나머지 모두!)

Falsy가 아닌 값은 **전부 Truthy**로 간주됩니다:

| Truthy 값 예시     | 설명           |
| --------------- | ------------ |
| `"hello"`       | 비어 있지 않은 문자열 |
| `42`            | 0이 아닌 숫자     |
| `-999`          | 음수도 Truthy   |
| `{}`            | 빈 객체         |
| `[]`            | 빈 배열         |
| `function() {}` | 함수           |

```javascript
if ("hello")     → true
if ([])          → true
if ({})          → true
```

---

## 🧪 실습 예제

### ❓ Q1. 다음 조건은 true일까요?

```javascript
if ("0") console.log("실행됨");
```

✅ 실행됨 (`"0"`은 빈 문자열이 아니므로 Truthy)

---

### ❓ Q2. 다음 중 Falsy는?

```javascript
let a = "";
let b = " ";
let c = 0;
let d = [];
```

* `a`: ❌ 빈 문자열 → Falsy
* `b`: ✅ 공백 문자열 → Truthy
* `c`: ❌ 0 → Falsy
* `d`: ✅ 빈 배열 → Truthy

---

## 🧠 typeof와 혼동 주의

```javascript
console.log(typeof null);      // "object"
console.log(typeof undefined); // "undefined"
```

* `typeof`는 데이터 타입을 알려줄 뿐이고,
* `null`, `undefined`는 조건식에서 **Falsy**로 평가됩니다.

---

## 🛠️ 활용 예시

### 기본값 할당

```javascript
let username = input || "Guest";
```

* `input`이 Falsy(예: `null`, `""`, `undefined`)면 `"Guest"` 할당

### 조건 분기

```javascript
if (!value) {
  console.log("값이 비어있습니다");
}
```

---

## ❗ 헷갈리기 쉬운 사례

| 표현식                 | 평가 결과  | 설명             |
| ------------------- | ------ | -------------- |
| `"false"`           | Truthy | 문자열이므로 비어있지 않음 |
| `[]`                | Truthy | 비어 있어도 배열은 객체  |
| `{}`                | Truthy | 비어 있어도 객체      |
| `NaN`               | Falsy  | 수가 아님          |
| `null == undefined` | true   | 느슨한 동등 비교에서만 참 |

---

## 🔍 Falsy 검사 함수

```javascript
function isFalsy(value) {
  return !Boolean(value);
}

console.log(isFalsy(0));        // true
console.log(isFalsy("hello"));  // false
```

---

## 🧩 디버깅 팁

* `if (value)` 형태는 Truthy/Falsy 판별식이다.
* 명확한 판단을 위해 `=== null`, `=== undefined`, `=== 0` 같이 비교하는 것이 안전할 때도 있다.
* `Boolean(value)` 또는 `!!value`를 사용하면 명시적 불리언 변환 가능

---

## 📌 마무리 정리

| 항목          | Truthy / Falsy |
| ----------- | -------------- |
| `" "`       | ✅ Truthy       |
| `""`        | ❌ Falsy        |
| `[]`        | ✅ Truthy       |
| `{}`        | ✅ Truthy       |
| `null`      | ❌ Falsy        |
| `undefined` | ❌ Falsy        |
| `false`     | ❌ Falsy        |
| `42`        | ✅ Truthy       |
| `NaN`       | ❌ Falsy        |

