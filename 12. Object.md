# 🧠 JavaScript Object(객체)

---

## ✅ 1. 객체(Object)란?

> 자바스크립트에서 객체(Object)는 키(key)와 값(value)의 쌍으로 이루어진 **데이터 집합**입니다.
> 현실 세계의 개체(예: 사람, 자동차)를 속성(데이터)과 동작(함수)로 표현할 수 있습니다.

---

## 🏗️ 2. 객체 구조

```javascript
const user = {
  name: "Alice",
  age: 25,
  isAdmin: false,
  greet: function () {
    console.log("Hello, " + this.name);
  }
};
```

| 요소        | 설명          |
| --------- | ----------- |
| `name`    | 키 (속성명)     |
| `"Alice"` | 값 (문자열 데이터) |
| `greet()` | 메서드 (함수형 값) |

---

## 🧰 3. 객체 생성 방법

### ① 객체 리터럴 (가장 일반적인 방법)

```javascript
const person = {
  name: "John",
  age: 30
};
```

```javascript
person.name = "kris";         // 가능
person.age = 'kim';           // 가능
```
const는 객체 내부 변경을 막지 않음.
단지 user라는 변수 자체에 다른 객체를 다시 대입할 수 없을 뿐.

```javascript
user = {}; // ❌ 오류 (재할당 불가)
```

### ② `new Object()` 생성자

```javascript
const person = new Object();
person.name = "John";
person.age = 30;
```

### ③ 생성자 함수 사용

```javascript
function Person(name, age) {
  this.name = name;
  this.age = age;
}

const p = new Person("Jane", 22);
```

### ④ 클래스(Class) 사용

```javascript
class User {
  constructor(name) {
    this.name = name;
  }
  greet() {
    console.log("Hi " + this.name);
  }
}
```

---

## 🔍 4. 속성 접근 방법

### ✅ 점 표기법

```javascript
console.log(user.name); // Alice
```

### ✅ 대괄호 표기법 (동적 접근 시 유용)

```javascript
let key = "age";
console.log(user[key]); // 25
```

---

## 🛠️ 5. 속성 추가, 수정, 삭제

```javascript
user.email = "alice@example.com"; // 추가
user.age = 26; // 수정
delete user.isAdmin; // 삭제
```

---

## 🧠 6. 객체 메서드(Method)

객체 안의 **함수형 값**을 메서드라고 부릅니다.

```javascript
const car = {
  brand: "Hyundai",
  drive() {
    console.log(this.brand + " is driving!");
  }
};

car.drive(); // Hyundai is driving!
```

---

## 🧪 7. 실습 예제

### Q1. 객체 생성 및 속성 접근

```javascript
const book = {
  title: "JavaScript Guide",
  author: "Mozilla"
};

console.log(book.title); // ?
```

<details><summary>정답 보기</summary>
출력: JavaScript Guide
</details>

---

### Q2. 동적 키 사용

```javascript
const field = "email";
const user = {
  name: "Anna",
  [field]: "anna@mail.com"
};

console.log(user.email); // ?
```

<details><summary>정답 보기</summary>
출력: anna@mail.com
</details>

---

## 🔧 8. 리팩토링: 중복 제거

### Before

```javascript
const car = {
  brand: "Hyundai",
  speed: 0,
  accelerate: function () {
    this.speed += 10;
  },
  brake: function () {
    this.speed -= 5;
  }
};
```

### After (메서드 축약)

```javascript
const car = {
  brand: "Hyundai",
  speed: 0,
  accelerate() {
    this.speed += 10;
  },
  brake() {
    this.speed -= 5;
  }
};
```

---

## 🐛 9. 디버깅 팁

* `this` 키워드는 객체의 **문맥(context)** 을 정확히 이해해야 함
* `undefined`가 나올 경우, 잘못된 key 접근 또는 `this` 바인딩 오류일 수 있음
* 객체 복사 시 얕은 복사 vs 깊은 복사 주의

---

## 📚 10. 보충 학습

| 주제                 | 설명                    |
| ------------------ | --------------------- |
| `Object.keys()`    | 모든 키 배열 반환            |
| `Object.values()`  | 모든 값 배열 반환            |
| `Object.entries()` | \[key, value] 쌍 배열 반환 |
| `Object.assign()`  | 객체 복사                 |
| `JSON.stringify()` | 객체 → JSON             |
| `JSON.parse()`     | JSON → 객체             |
| 얕은 복사 / 깊은 복사      | 참조 vs 재귀적 복사 차이       |

---

## 🎯 마무리 요약

| 개념        | 설명              |
| --------- | --------------- |
| 객체        | 키-값 쌍의 집합       |
| 메서드       | 객체 내 함수형 값      |
| 속성 접근     | 점/대괄호 방식        |
| 동적 속성     | 계산된 키 이름 사용     |
| 생성 방식 다양성 | 리터럴, 생성자, 클래스 등 |


* 🔁 **객체의 반복과 순회 (`for...in`, `Object.keys()` 등)**
* 🧬 **객체 복사와 구조 분해 할당**
* 🔐 **객체의 접근 제어 (`freeze`, `seal`, `defineProperty`)**
* 🧱 **객체를 활용한 모듈 설계와 캡슐화 패턴**

