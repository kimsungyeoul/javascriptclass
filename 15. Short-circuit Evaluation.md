# ⚡ JavaScript 단락 평가 (Short-circuit Evaluation)

---

## ✅ 단락 평가란?

> 논리 연산자 `||` (OR) 또는 `&&` (AND)를 사용할 때, **왼쪽 피연산자의 값만으로 결과가 확정되면 오른쪽은 평가하지 않고 생략**하는 자바스크립트의 동작 방식입니다.

이것은 단순한 "논리 연산" 그 이상으로, **값의 리턴 및 흐름 제어**에도 사용됩니다.

---

## 🧠 기본 문법과 평가 규칙

### `||` (OR) 연산자

> 왼쪽 값이 **Truthy**면 → 그 값을 반환
> 그렇지 않으면 → 오른쪽 값을 평가하여 반환

```javascript
A || B → A가 Truthy면 A 반환, Falsy면 B 반환
```

### `&&` (AND) 연산자

> 왼쪽 값이 **Falsy**면 → 그 값을 반환
> 그렇지 않으면 → 오른쪽 값을 평가하여 반환

```javascript
A && B → A가 Falsy면 A 반환, Truthy면 B 반환
```

---

## 🧪 실습 예제

### 예제 1: OR `||`

```javascript
console.log("Hello" || "World"); // "Hello"
console.log("" || "World");      // "World"
console.log(null || 0 || "JS");  // "JS"
```

### 예제 2: AND `&&`

```javascript
console.log("Hello" && "World"); // "World"
console.log(null && "World");    // null
console.log("JS" && 0 && "CSS"); // 0
```

---

## 🔥 실전 활용 예제

### ① 디폴트값 설정

```javascript
const name = inputName || "Guest";
```

* `inputName`이 Falsy (예: null, undefined, "")일 경우 `"Guest"` 사용

---

### ② 조건 실행

```javascript
isLoggedIn && showDashboard();
```

* `isLoggedIn`이 true일 때만 `showDashboard()` 실행

```javascript
isAdmin || alert("권한이 없습니다.");
```

* `isAdmin`이 false면 `alert()` 실행됨

---

## ⚠️ 주의할 점

| 표현식            | 결과     | 설명                     |        |                       |
| -------------- | ------ | ---------------------- | ------ | --------------------- |
| \`false        |        | true\`                 | `true` | 첫 번째는 Falsy → 두 번째 평가 |
| `"hi" && 0`    | `0`    | 첫 번째는 Truthy → 두 번째 반환 |        |                       |
| `null && "hi"` | `null` | 첫 번째는 Falsy → 바로 반환    |        |                       |

---

## 💡 팁: Boolean 강제 변환

```javascript
!!value  // → Boolean(value)와 동일
```

```javascript
console.log(!!"Hello"); // true
console.log(!!0);        // false
```

단락 평가를 조합하면 복잡한 조건을 짧게 표현할 수 있습니다.

---

## 🧱 복합 예제

```javascript
function greet(user) {
  const name = user && user.name || "Anonymous";
  console.log("Hello, " + name);
}
```

* `user`가 null이면 → `"Anonymous"`
* `user.name`이 없으면 → `"Anonymous"`
* 둘 다 있으면 → 해당 이름 출력

---

## 🛠️ 리팩토링 전후

### Before

```javascript
if (config && config.enabled) {
  runTask();
}
```

### After

```javascript
config?.enabled && runTask(); // 단락 평가 + 옵셔널 체이닝
```

---

## 🐛 디버깅 포인트

* `||`는 **디폴트값 설정**으로 자주 쓰이지만, 0, ""도 Falsy로 처리되므로 **실수 주의**
* `&&`는 **조건부 실행**으로 사용 시, **오른쪽에 함수 호출 시 부작용 조심**
* `console.log(value || "fallback")`으로 단락 평가 디버깅 가능

---

## 📌 요약 비교

| 연산자  | 평가 규칙               | 예시           | 반환값                  |     |   |       |       |
| ---- | ------------------- | ------------ | -------------------- | --- | - | ----- | ----- |
| \`   |                     | \`           | 첫 번째 Truthy → 그 값 반환 | \`0 |   | "A"\` | `"A"` |
| `&&` | 첫 번째 Falsy → 그 값 반환 | `"A" && "B"` | `"B"`                |     |   |       |       |



---

## ✅ 실습 과제

### Q1. 다음 코드의 출력은?

```javascript
console.log(false || 0 || null || "hi" || undefined);
```

<details><summary>정답 보기</summary>
출력: `"hi"` (앞의 모든 값이 Falsy)
</details>

