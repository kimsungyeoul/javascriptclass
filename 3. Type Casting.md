# 🧑‍🏫 JavaScript 타입 캐스팅(Type Casting)

---

## ✅ 1. 타입 캐스팅이란?

> **타입 캐스팅(Type Casting)** 또는 **타입 변환(Type Conversion)** 이란,
> 한 자료형의 값을 다른 자료형으로 **변환하는 행위**를 말합니다.

자바스크립트는 **동적 타입 언어**이므로,
값의 자료형을 **명시적(explicit)** 또는 **암묵적(implicit)** 으로 바꿀 수 있습니다.

---

## 🧩 2. 타입 캐스팅의 종류

| 구분                    | 설명                                             |
| --------------------- | ---------------------------------------------- |
| **명시적 변환 (Explicit)** | 프로그래머가 의도적으로 타입을 바꿈 (`String()`, `Number()` 등) |
| **암묵적 변환 (Implicit)** | 자바스크립트 엔진이 자동으로 변환 (`+`, `==`, `if` 조건 등)      |

---

## 📘 3. 명시적 타입 변환 (Explicit Casting)

### ✅ 문자열로 변환

```javascript
let var1 = String(123);      // 정수 리터럴 123을 문자열 "123"으로 변경
let var2 = String(true);     // "true"
let var3 = String(null);     // "null"
let var4 = String(undefined);// "undefined"
let var5 = (123).toString(); // "123"
```

### ✅ 숫자로 변환

```javascript
let var1 = Number("123");    // 문자열 리터럴 '123'을 정수 123으로 변경
let var2 = Number("3.14");   // 3.14
let var3 = Number("abc");    // NaN
let var4 = Number(true);     // 1
let var5 = Number(false);    // 0
```

### ✅ 불리언으로 변환

```javascript
Boolean(0);       // false
Boolean("");      // false
Boolean(null);    // false
Boolean("hello"); // true
Boolean(123);     // true
```

---

## 🚧 4. 암묵적 타입 변환 (Implicit Casting)

> 연산자 또는 조건문에 의해 자바스크립트가 자동으로 변환

### ✅ 문자열 컨텍스트

```javascript
"5" + 1     // "51"   → 숫자가 문자열로 변환됨
"5" - 1     // 4      → 문자열이 숫자로 변환됨
"5" * "2"   // 10     → 둘 다 숫자로 변환됨
```

### ✅ 비교 컨텍스트

```javascript
5 == "5"     // true   → "5"가 숫자로 변환됨
0 == false   // true   → false → 0
null == undefined // true
```

❗ `==`는 암묵적 타입 변환이 일어남 → 예측이 어려움

✅ `===` (strict equality)를 사용하면 **타입까지 일치해야 true**

```javascript
5 === "5"    // false
0 === false  // false
```

---

## 🧠 5. Truthy vs Falsy
**Truthy(참 같은 값)** 와 **Falsy(거짓 같은 값)** 는 자바스크립트가 조건문(if문 등)에서 데이터를 판단하는 독특한 방식입니다.

쉽게 말해, 불리언(Boolean) 타입의 true나 false가 아니더라도, "이 정도면 참으로 봐줄게" 혹은 **"이건 데이터가 없는 셈이니 거짓으로 볼게"** 라고 판별하는 규칙입니다.

| Falsy 값             | 설명               |
| ------------------- | ---------------- |
| `false`             | 불리언 false 값      |
| `0`, `-0`, `0n`     | 숫자 0 또는 BigInt 0 |
| `""`                | 빈 문자열            |
| `null`, `undefined` | 없음               |
| `NaN`               | 숫자 아님            |

그 외의 모든 값은 **Truthy**로 평가됨

### ✅ 예시 
#### 1. Falsy 5형제 확인하기
```javascript
const falsyValues = [false, 0, "", null, undefined, NaN];

falsyValues.forEach(value => {
    if (!value) {
        console.log(`[${value}] 은(는) Falsy입니다.`);
    }
});
```

#### 2. 실무 예제: "데이터가 비어있는지 확인하기"
```javascript
function checkData(data) {
    if (data) {
        console.log("✅ 데이터를 성공적으로 수신했습니다:", data);
    } else {
        console.log("❌ 데이터가 없거나 유효하지 않습니다.");
    }
}

checkData("Tom");     // ✅ (문자열은 Truthy)
checkData("");        // ❌ (빈 문자열은 Falsy)
checkData(undefined); // ❌ (값이 없음은 Falsy)
```

#### 3. Truthy의 반전: "비어있는데 참(True)이라고?"
```javascript
const items = [];

if (items) {
    console.log("장바구니가 존재합니다."); // 출력됨!
}

// 💡 팁: 배열이 비었는지 확인하려면 .length를 써서 '0(Falsy)'을 만들어야 합니다.
if (items.length) {
    console.log("장바구니에 물건이 있습니다.");
} else {
    console.log("장바구니가 비었습니다."); // 0은 Falsy이므로 이쪽이 실행됨
}
```

#### 4. 단축 평가(Short-circuit) 활용
```javascript
let userName = ""; // 사용자 이름을 입력받지 못한 상황

// userName이 Falsy("")면 뒤의 "익명"을 선택함
let displayName = userName || "익명"; 

console.log(displayName); // "익명"
```



---

## ⚖️ 6. 비교 연산과 타입 변환

| 표현식                 | 결과    | 이유                   |
| ------------------- | ----- | -------------------- |
| `"5" == 5`          | true  | "5" → 5 변환           |
| `false == 0`        | true  | false → 0 변환         |
| `null == undefined` | true  | 특수 규칙                |
| `NaN == NaN`        | false | NaN은 자기 자신과도 다름      |
| `[] == false`       | true  | \[] → "" → 0 → false |
| `[] == 0`           | true  | \[] → "" → 0         |

---

## 🔥 7. 특이한 변환 사례

```javascript
Boolean([]);         // true
Boolean({});         // true
Number(null);        // 0
Number(undefined);   // NaN
[] + []              // "" (빈 문자열)
[] + {}              // "[object Object]"
{} + []              // 0  ← 주의! 표현식으로 해석됨
```

---

## 🧪 8. 실습 문제

### Q1. 다음 코드의 출력 결과는?

```javascript
console.log("5" + 1);
console.log("5" - 1);
console.log(true + 1);
console.log(null + 1);
console.log(undefined + 1);
```

<details>
<summary>✅ 정답 보기</summary>

"51"
4
2
1
NaN

</details>

---

### Q2. 다음 중 `true`로 평가되는 값은?

```javascript
Boolean("false")
Boolean(0)
Boolean("0")
Boolean(null)
Boolean({})
```

<details>
<summary>✅ 정답 보기</summary>

"false" → true
"0"     → true
{}      → true
(나머지는 false)

</details>

---

## 🎯 9. 정리

| 대상          | 문자열 변환             | 숫자 변환 | 불리언 변환 |
| ----------- | ------------------ | ----- | ------ |
| `"123"`     | "123"              | 123   | true   |
| `true`      | "true"             | 1     | true   |
| `false`     | "false"            | 0     | false  |
| `null`      | "null"             | 0     | false  |
| `undefined` | "undefined"        | NaN   | false  |
| `{}`        | "\[object Object]" | NaN   | true   |
| `[]`        | ""                 | 0     | true   |

---

