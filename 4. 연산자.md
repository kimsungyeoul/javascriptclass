# 🧑‍🏫 JavaScript 연산자(Operators)

---

## 📌 1. 연산자란?

> **연산자(Operator)** 는 하나 이상의 피연산자(operand)에 대해 연산을 수행하여 결과를 반환하는 기호 또는 키워드입니다.

---

## 🧩 2. 자바스크립트의 연산자 종류

| 분류                 | 예시                                             | 설명        |         |        |
| ------------------ | ---------------------------------------------- | --------- | ------- | ------ |
| 산술 연산자             | `+`, `-`, `*`, `/`, `%`, `**`, `++`, `--`      | 숫자 계산     |         |        |
| 비교 연산자             | `==`, `!=`, `===`, `!==`, `>`, `<`, `>=`, `<=` | 값을 비교     |         |        |
| 논리 연산자             | `&&`, \`                                       |           | `, `!\` | 논리값 결합 |
| 할당 연산자             | `=`, `+=`, `-=`, `*=`, `/=`, `%=`              | 값을 변수에 저장 |         |        |
| 문자열 연결             | `+`                                            | 문자열 결합    |         |        |
| 삼항 연산자             | `조건 ? 값1 : 값2`                                 | 간단한 조건 처리 |         |        |
| typeof, instanceof | `typeof`, `instanceof`                         | 타입 검사     |         |        |

---

## ✅ 3. 산술 연산자 실습

```javascript
let a = 10;
let b = 3;

console.log("덧셈:", a + b);       // 13
console.log("뺄셈:", a - b);       // 7
console.log("곱셈:", a * b);       // 30
console.log("나눗셈:", a / b);     // 3.333...
console.log("나머지:", a % b);     // 1
console.log("거듭제곱:", a ** b);  // 1000
```

---

## ✅ 4. 문자열 연결 실습

```javascript
let name = "Alice";
let age = 25;

console.log("이름: " + name + ", 나이: " + age); // 이름: Alice, 나이: 25
```

---

## ✅ 5. 비교 연산자 실습

```javascript
console.log(5 == "5");   // true  (값만 비교)
console.log(5 === "5");  // false (값 + 타입 비교)
console.log(5 != "5");   // false
console.log(5 !== "5");  // true
console.log(10 > 5);     // true
console.log(10 <= 10);   // true
```

---

## ✅ 6. 논리 연산자 실습

```javascript
let x = true;
let y = false;

console.log(x && y); // false
console.log(x || y); // true
console.log(!x);     // false
```

---

## ✅ 7. 할당 연산자 실습

```javascript
let a = 5;

a += 2;  // a = a + 2 → 7
a *= 3;  // a = a * 3 → 21

console.log(a); // 21
```

---

## ✅ 8. 삼항 연산자 실습

```javascript
let score = 85;
let grade = score >= 90 ? "A" : "B";
console.log("성적 등급:", grade); // B
```

---

## ✅ 9. typeof & instanceof 실습

```javascript
console.log(typeof 123);          // "number"
console.log(typeof "hello");      // "string"
console.log(typeof true);         // "boolean"
console.log(typeof {});           // "object"
console.log(typeof function(){}); // "function"

let arr = [];
console.log(arr instanceof Array);  // true
console.log(arr instanceof Object); // true
```

---

## 🧪 10. 실습 퀴즈

### Q1. 다음 코드의 출력 결과는?

```javascript
let a = "5";
let b = 2;
console.log(a + b);  // ?
console.log(a - b);  // ?
```

<details>
<summary>✅ 정답 보기</summary>

* `"5" + 2` → `"52"` (문자열 연결)
* `"5" - 2` → `3` (문자열이 숫자로 변환되어 연산)

</details>

---

### Q2. 논리 연산자 결과는?

```javascript
console.log(0 || "default"); // ?
console.log("hi" && 123);    // ?
```

<details>
<summary>✅ 정답 보기</summary>

* `0 || "default"` → `"default"` (0은 falsy)
* `"hi" && 123` → `123` (앞이 truthy면 뒤 값 반환)

</details>

---

### Q3. 다음 중 `true`가 출력되는 것은?

A. `"0" == 0`
B. `"0" === 0`
C. `null == undefined`
D. `NaN == NaN`

<details>
<summary>✅ 정답 보기</summary>

정답: A, C

* `"0" == 0` → true (타입 변환 후 비교)
* `"0" === 0` → false
* `null == undefined` → true
* `NaN == NaN` → false (NaN은 자기 자신과도 다르다)

</details>

---

## 🎯 마무리 요약

| 연산자 분류 | 대표 예시                  | 설명          |         |       |
| ------ | ---------------------- | ----------- | ------- | ----- |
| 산술     | `+`, `-`, `*`          | 수학 계산       |         |       |
| 문자열 연결 | `+`                    | 문자열 이어붙이기   |         |       |
| 비교     | `==`, `===`, `>`       | 값/타입 비교     |         |       |
| 논리     | `&&`, \`               |             | `, `!\` | 논리 연산 |
| 할당     | `=`, `+=`, `*=`        | 변수에 값 저장/연산 |         |       |
| 조건     | `조건 ? 참 : 거짓`          | 삼항 연산자      |         |       |
| 기타     | `typeof`, `instanceof` | 타입/객체 판별    |         |       |

