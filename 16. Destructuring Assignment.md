# 📦 JavaScript 구조 분해 할당 (Destructuring Assignment)

---

## ✅ 구조 분해 할당이란?

> 객체나 배열에서 값을 추출하여, **개별 변수에 손쉽게 할당**할 수 있는 문법입니다.

---

## 🧩 1. 배열 구조 분해 (Array Destructuring)

### ✅ 기본 사용법

```javascript
const arr = [10, 20, 30];
const [a, b, c] = arr;

console.log(a); // 10
console.log(b); // 20
console.log(c); // 30
```

### ✅ 일부만 추출

```javascript
const arr = [100, 200, 300];
const [first, , third] = arr;
console.log(third); // 300
```

### ✅ 나머지 항목 저장 (Rest)

```javascript
const arr = [1,2,3,4];
const [head, ...rest] = arr;
console.log(rest); // [2, 3, 4]
```

### ✅ 교환(swap)

```javascript
let x = 1, y = 2;
[x, y] = [y, x];
console.log(x, y); // 2 1
```

---

## 🧩 2. 객체 구조 분해 (Object Destructuring)

---

# 1️⃣ 기본 문법

```
const { prop } = object;
```

### 의미:

* object.prop → 변수 prop

---

# 2️⃣ 변수 이름 변경 (Rename)

```
const { prop: newName } = object;
```

### 의미:

* object.prop → newName

📌 *이때 `{ prop: newName }`은 객체 생성이 아니다.
단지 “prop을 newName 변수에 바인딩하라”는 패턴이다.*

---

# 3️⃣ 기본값(Default Value)

```
const { prop = defaultValue } = object;
```

### 의미:

* object.prop 없으면 defaultValue 사용

---

# 4️⃣ 이름 변경 + 기본값

```
const { prop: newName = defaultValue } = object;
```

---

# 5️⃣ 중첩 구조 분해 (Nested Destructuring)

```
const { outer: { inner } } = object;
```

여기서 `outer:`는 패턴 진입용 키이며
변수 outer는 생기지 않는다.

---

# 6️⃣ 나머지 속성(Rest Properties)

```
const { prop1, ...rest } = object;
```

---

# 7️⃣ 선택적 프로퍼티 구조 분해(Optional)

객체에 key가 없어도 에러 X

```
const { notExist } = object;  
// undefined
```

---

# 8️⃣ 함수 파라미터에서의 구조 분해

```
function fn({ prop, prop2 }) { }
```

---

# 9️⃣ 패턴 안에서의 `:` 의미

```
{ key: newName }
```

✓ 구조 분해 문법에서 `:`는 **“key의 값을 newName 변수에 바인딩”**
✗ 객체 리터럴의 `"key: value"` 아님

---

# 🔟 전체 문법 BNF 스타일 요약

```
ObjectBindingPattern :
    { BindingPropertyList }
    { BindingPropertyList , }
    { }

BindingPropertyList :
    BindingProperty
    BindingPropertyList , BindingProperty

BindingProperty :
    Identifier
    Identifier Initializer
    PropertyName : BindingIdentifier
    PropertyName : BindingIdentifier Initializer
    PropertyName : ObjectBindingPattern
    PropertyName : ArrayBindingPattern
```

---

# 🎯 한 줄 요약 (핵심 문법만)

```
const {
  key,               // 동일 변수 이름
  key: newName,      // 변수 이름 변경
  key = defaultVal,  // 기본값
  key: newName = v,  // 이름 변경 + 기본값
  nested: { inner }, // 중첩 구조
  ...rest            // 나머지
} = object;
```

---


### ✅ 기본 사용법

```javascript
const user = { id: 101, nickname: "intheeast" };
const { id, nickname } = user;
console.log(id);       // 101
console.log(nickname); // "intheeast"
```

### ✅ 별칭 (Alias)

```javascript
// 다음 코드는 “객체 리터럴”이 아님.
// 이것은 “구조 분해 패턴(Destructuring Pattern)”
// 오른쪽에 있는 객체(user)로부터 값을 꺼내서 왼쪽 패턴에 맞게 변수에 할당하라는 의미.
const { id: userId } = user;
// 위 코드는 다음과 같습니다.
// const userId = user.id;
console.log(userId); // 101
```

### ✅ 디폴트값 설정

```javascript
const { age = 30 } = { name: "Eve" };
console.log(age); // 30
```

---

## 🧩 3. 함수 파라미터에서 구조 분해

### ✅ 객체 파라미터

```javascript
function printUser({ name, age }) {
  console.log(`${name} (${age})`);
}

const user = { name: "David", age: 28 };
printUser(user); // David (28)
```

### ✅ 배열 파라미터

```javascript
function sum([a, b]) {
  return a + b;
}

console.log(sum([5, 10])); // 15
```

---

## 🧩 4. 중첩 구조 분해

```javascript
const user = {
  id: 1,
  profile: {
    name: "Jane",
    contact: { email: "jane@example.com" }
  }
};

const {
  profile: {
    contact: { email }
  }
} = user;

console.log(email); // "jane@example.com"
```

---

## 🧪 실습 예제

### Q1. 다음 코드의 출력 결과는?

```javascript
const [a, , b] = [10, 20, 30];
console.log(a, b);
```

<details><summary>정답 보기</summary>
출력: `10 30`
</details>

---

### Q2. 함수 파라미터 구조 분해 실습

```javascript
function greet({ name = "Guest" }) {
  console.log("Hello " + name);
}

greet({ name: "Sara" }); // Hello Sara
greet({});               // Hello Guest
```

---

## ⚠️ 주의 사항

| 상황                      | 설명                                          |
| ----------------------- | ------------------------------------------- |
| undefined 또는 null 대상일 때 | 객체 구조 분해 시 에러 발생 가능                         |
| 배열 인덱스 범위 초과            | 값이 undefined로 할당됨                           |
| 디폴트값과 별칭 동시 사용 시         | `const { a: alias = default } = obj` 형태로 작성 |

---

## 🛠️ 디버깅 팁

* `TypeError: Cannot destructure property` 에러가 난다면:

  * 구조 분해 대상이 `undefined`거나 `null`인지 확인할 것
* 디버깅 시 `console.log()`를 통해 추출된 변수 값을 확인

---

## 📘 고급 팁

### ✅ for...of + 구조 분해

```javascript
const people = [
  { name: "Lee", age: 30 },
  { name: "Kim", age: 25 }
];

for (const { name, age } of people) {
  console.log(`${name}: ${age}`);
}
```

---

### ✅ default + alias + nested

```javascript
const config = {
  port: 8080,
  server: { host: "localhost" }
};

const {
  port: p = 3000,
  server: { host: h = "127.0.0.1" }
} = config;

console.log(p, h); // 8080 "localhost"
```

---

## ✅ 요약 정리

| 구문                      | 설명            |
| ----------------------- | ------------- |
| `[a, b] = [1, 2]`       | 배열 구조 분해      |
| `{x, y} = obj`          | 객체 구조 분해      |
| `function f({a, b}) {}` | 함수 파라미터 구조 분해 |
| `const [a, ...rest]`    | 나머지 요소 배열로 받기 |
| `const {x: alias}`      | 별칭 (alias) 사용 |
| `const {x = default}`   | 디폴트값 설정        |

