# 🧑‍🏫 JavaScript 변수(Variable)와 상수(Constant)

---

## 📌 1. 변수(Variable)란?

> **변수는 데이터를 저장하는 이름이 붙은 공간**입니다.
> 자바스크립트에서는 `var`, `let`, `const` 키워드로 변수를 선언할 수 있습니다.

```javascript
let age = 30;
const name = "Alice";
var job = "developer";
```

---

## ✅ 2. 변수 선언 키워드 3종 비교

| 키워드     | 스코프    | 재할당  | 재선언  | 호이스팅              | 사용 추천도          |
| ------- | ------ | ---- | ---- | ----------------- | --------------- |
| `var`   | 함수 스코프 | ✅ 가능 | ✅ 가능 | O (초기값 undefined) | ❌ 거의 사용하지 않음    |
| `let`   | 블록 스코프 | ✅ 가능 | ❌ 불가 | O (TDZ 발생)        | ✅ 일반 변수에 사용     |
| `const` | 블록 스코프 | ❌ 불가 | ❌ 불가 | O (TDZ 발생)        | ✅ 변경 불가능한 값에 사용 |

---

## 📦 3. `var`의 문제점

```javascript
if (true) {
  var x = 10;
}
console.log(x); // ✅ 10 (함수 스코프)
```

* `var`는 블록({})을 무시하고 **함수 전체에서 접근 가능**
* 중복 선언도 가능 → 코드 안정성 저하

```javascript
var x = 1;
var x = 2; // ✅ 문제 없음
```

---

## ✅ 4. `let`과 `const`의 도입 (ES6 이후)

```javascript
let age = 25;
age = 30; // ✅ 재할당 가능

const name = "Bob";
name = "Alice"; // ❌ TypeError: Assignment to constant variable
```

* `let`은 **재할당 가능**, `const`는 **재할당 불가능**
* **두 키워드 모두 블록 스코프**를 따름
* **중복 선언 불가**

---

## 🧊 5. `const`는 불변인가? (주의점)

```javascript
const user = { name: "Tom" };
user.name = "Jerry"; // ✅ 가능

user = { name: "Bob" }; // ❌ TypeError
```

* `const`는 변수 자체의 **참조값 변경이 금지**될 뿐
* **객체 내부의 값은 변경 가능**

➡ 진짜 "불변 객체"로 만들려면 `Object.freeze()`를 사용해야 함

```javascript
const frozen = Object.freeze({ name: "Lee" });
frozen.name = "Kim"; // 무시됨
```

---

## 📚 6. 스코프(Scope) 정리

| 키워드     | 전역 | 함수 | 블록 |
| ------- | -- | -- | -- |
| `var`   | ✅  | ✅  | ❌  |
| `let`   | ✅  | ✅  | ✅  |
| `const` | ✅  | ✅  | ✅  |

* `var`는 함수 스코프 → 블록 무시
* `let`, `const`는 **블록 스코프** → if, for, while 안에서 독립

---

## 🧪 7. 실습 문제

### Q1. 다음 코드의 결과는?

```javascript
if (true) {
  let a = 10;
}
console.log(a);
```

<details>
<summary>정답 보기</summary>
❌ ReferenceError (블록 스코프 때문)
</details>

---

### Q2. 다음 중 오류가 발생하는 것은?

```javascript
const x = [1, 2, 3];
x[0] = 99;     // ?
x = [9, 8, 7]; // ?
```

<details>
<summary>정답 보기</summary>

* `x[0] = 99` → ✅ 가능 (객체 내부 값 변경 가능)
* `x = [9, 8, 7]` → ❌ TypeError (참조 자체 변경은 불가)

</details>

---

## 🧭 8. 변수 선언 키워드 선택 가이드

| 상황              | 추천 키워드            |
| --------------- | ----------------- |
| 재할당 불필요한 상수, 객체 | `const`           |
| 재할당이 필요한 변수     | `let`             |
| 예전 코드와의 호환만 필요  | `var` (❌ 권장하지 않음) |

---

## 🎯 정리

| 항목     | `var`       | `let` | `const` |
| ------ | ----------- | ----- | ------- |
| 스코프    | 함수          | 블록    | 블록      |
| 재할당    | 가능          | 가능    | ❌ 불가능   |
| 재선언    | 가능          | ❌ 불가능 | ❌ 불가능   |
| 전역 등록  | `window.변수` | ❌     | ❌       |
| TDZ 발생 | ❌ 없음        | ✅ 있음  | ✅ 있음    |

---

# 🧪 JavaScript 변수와 상수 실습 예제

---

## 📌 실습 1: `var`의 함수 스코프 확인

```javascript
function testVar() {
  if (true) {
    var x = 10;
  }
  console.log("var x =", x); // 예측 결과?
}

testVar();
```

✅ 예상 출력:

```
var x = 10
```

❗블록 스코프 무시 → 함수 전체에서 유효

---

## 📌 실습 2: `let`의 블록 스코프 확인

```javascript
function testLet() {
  if (true) {
    let y = 20;
  }
  console.log("let y =", y); // 예측 결과?
}

testLet();
```

✅ 예상 오류:

```
ReferenceError: y is not defined
```

---

## 📌 실습 3: `const` 재할당 불가 확인

```javascript
const PI = 3.14;
console.log("원주율:", PI);

PI = 3.14159; // 예측 결과?
```

✅ 예상 오류:

```
TypeError: Assignment to constant variable.
```

---

## 📌 실습 4: `var`, `let`, `const`의 호이스팅 차이

```javascript
console.log(a); // ?
var a = 1;

console.log(b); // ?
let b = 2;

console.log(c); // ?
const c = 3;
```

✅ 예상 결과:

```
undefined     // var은 호이스팅됨 (초기값 undefined)
ReferenceError // let → TDZ
ReferenceError // const → TDZ
```

---

## 📌 실습 5: `const` 객체 내부 변경 가능성

```javascript
const user = { name: "Alice" };

user.name = "Bob";
console.log(user.name); // 예측 결과?

user = { name: "Charlie" }; // 예측 결과?
```

✅ 예상 출력:

```
Bob
TypeError: Assignment to constant variable.
```

➡ 프로퍼티 변경은 가능하지만, 참조 자체 변경은 불가

---

## 📌 실습 6: 중복 선언 비교

```javascript
var x = 10;
var x = 20; // 가능

let y = 10;
let y = 20; // 불가능 → 예측 결과?

const z = 30;
const z = 40; // 불가능 → 예측 결과?
```

✅ 예상 오류:

```
SyntaxError: Identifier 'y' has already been declared
SyntaxError: Identifier 'z' has already been declared
```

---

## 📌 실습 7: 전역 객체에 포함 여부 확인 (`window`)

```javascript
var a = 100;
let b = 200;
const c = 300;

console.log(window.a); // ?
console.log(window.b); // ?
console.log(window.c); // ?
```

✅ 예상 결과 (브라우저 환경 기준):

```
100
undefined
undefined
```

---

## 📌 실습 8: `for` 루프 안에서의 `var` vs `let` 차이

```javascript
for (var i = 0; i < 3; i++) {
  setTimeout(() => console.log("var i:", i), 100);
}

for (let j = 0; j < 3; j++) {
  setTimeout(() => console.log("let j:", j), 100);
}
```

✅ 예상 출력:

```
var i: 3
var i: 3
var i: 3
let j: 0
let j: 1
let j: 2
```

➡ `let`은 각 반복마다 새로운 스코프를 가짐 → 클로저와 변수 캡처에 안전

---

## ✅ 정리 퀴즈 (강의 후 복습용)

### Q1. 다음 중 에러 없이 실행되는 코드는?

A.

```javascript
const x = 10;
x = 20;
```

B.

```javascript
let y = 5;
y = 10;
```

C.

```javascript
var z = 1;
var z = 2;
```

<details>
<summary>정답 보기</summary>

정답: B, C
A는 `const` 재할당 불가이므로 TypeError 발생

</details>


