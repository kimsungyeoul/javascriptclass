# 🧑‍🏫 JavaScript 화살표 함수 (Arrow Function)

---

## ✅ 1. 화살표 함수란?

> 화살표 함수(Arrow Function)는 ES6에서 도입된 새로운 함수 문법입니다.
> `function` 키워드를 사용하지 않고 **`=>` 기호**를 이용해 함수를 작성합니다.

```javascript
const greet = (name) => {
  console.log("Hello, " + name);
};
```

---

## 🧠 2. 왜 사용하나요?

| 장점        | 설명                    |
| --------- | --------------------- |
| 문법 간결     | 한 줄 함수 정의에 적합         |
| 가독성 향상    | 콜백 함수 작성 시 명료함        |
| `this` 고정 | `this`가 외부 컨텍스트로 바인딩됨 |

---

## 📘 3. 기본 문법 형식

```javascript
const 함수이름 = (파라미터1, 파라미터2) => {
  // 실행 코드
  return 반환값;
};
```

---

## 🔹 4. 예제: 일반 함수 → 화살표 함수 변환

### 일반 함수

```javascript
const add = function(a, b) {
  return a + b;
};
```

### 화살표 함수

```javascript
const add = (a, b) => {
  return a + b;
};
```

---

## ✨ 5. 문법 단축 (한 줄 리턴)

```javascript
const square = x => x * x;
```

✅ 파라미터가 하나면 `()` 생략 가능
✅ 한 줄 리턴이면 `{}`와 `return`도 생략 가능

---

## 🧊 6. 파라미터가 없을 때

```javascript
const hello = () => console.log("안녕하세요!");
```

---

## 🧪 7. 실습 예제

### Q1. 이름을 받아 “\~님 어서오세요”를 출력하는 화살표 함수 만들기

<details>
<summary>정답 보기</summary>

```javascript
const welcome = name => console.log(`${name}님 어서오세요`);
```

</details>

---

### Q2. 두 수를 받아 곱한 결과를 반환하는 화살표 함수

<details>
<summary>정답 보기</summary>

```javascript
const multiply = (a, b) => a * b;
```

</details>

---

## ⚠️ 8. 화살표 함수와 일반 함수의 차이

| 항목             | 일반 함수         | 화살표 함수              |
| -------------- | ------------- | ------------------- |
| `this` 바인딩     | 호출한 객체에 따라 동적 | 외부 스코프(정적)로 고정      |
| `arguments` 객체 | 존재함           | ❌ 없음 (`...args` 사용) |
| 생성자 사용 (`new`) | ✅ 가능          | ❌ 불가                |
| 문법             | `function` 사용 | `=>` 사용             |

---

### 예제: `this` 바인딩 차이

```javascript
const person = {
  name: "Eve",
  greet: function () {
    console.log("Hi, I'm " + this.name);
  },
  greetArrow: () => {
    console.log("Hi, I'm " + this.name);
  }
};

person.greet();       // Hi, I'm Eve
person.greetArrow();  // Hi, I'm undefined (this는 전역)
```

---

## 💬 9. 화살표 함수가 적합한 상황

| 상황                       | 추천         |
| ------------------------ | ---------- |
| 짧고 간단한 함수                | ✅          |
| 콜백 함수                    | ✅          |
| `this`, `arguments`가 필요함 | ❌ 일반 함수 사용 |
| 생성자 함수                   | ❌ 일반 함수 사용 |

---

## 🔧 10. 리팩토링 예제

### Before (일반 함수)

```javascript
setTimeout(function() {
  console.log("1초 후 실행");
}, 1000);
```

### After (화살표 함수)

```javascript
setTimeout(() => {
  console.log("1초 후 실행");
}, 1000);
```

➡ 가독성 향상, `this` 문제가 없으므로 콜백에 적합

---

## 🧠 11. 고급 주제

### 🔹 화살표 함수에는 `this`가 없다

```javascript
function Timer() {
  this.seconds = 0;
  setInterval(() => {
    this.seconds++;
    console.log(this.seconds);
  }, 1000);
}
new Timer(); // ✅ this가 Timer 인스턴스를 참조함
```

✅ 일반 함수였다면 this가 `undefined` 또는 `window`였을 수도 있음

---

## 📊 12. 마무리 요약

| 항목          | 설명                        |
| ----------- | ------------------------- |
| 문법          | `(파라미터) => { 실행코드 }`      |
| 리턴 생략       | 표현식 1줄이면 `return` 생략 가능   |
| `this`      | 외부 컨텍스트 고정 (lexical this) |
| `arguments` | 없음 (`...args` 사용)         |
| 생성자 사용      | 불가                        |
| 적합한 사용 상황   | 콜백, 짧은 함수, 이벤트 핸들러        |



